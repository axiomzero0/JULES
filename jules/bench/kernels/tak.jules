// bench: tak — deep recursion, 3-way nested calls
fn tak(x: i64, y: i64, z: i64) -> i64 {
    if y < x {
        return tak(tak(x - 1, y, z), tak(y - 1, z, x), tak(z - 1, x, y));
    }
    return z;
}

fn main() -> i64 {
    print(tak(33 as i64, 22 as i64, 12 as i64));
    return 0 as i64;
}
