// bench: fib — recursive call + branch heavy
fn fib(n: i64) -> i64 {
    if n < 2 { return n; }
    return fib(n - 1) + fib(n - 2);
}

fn main() -> i64 {
    print(fib(39 as i64));
    return 0 as i64;
}
