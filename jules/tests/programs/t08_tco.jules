// tail recursion: 1,000,000 frames without stack growth
fn count(n: i64, acc: i64) -> i64 {
    if n == 0 { return acc; }
    return count(n - 1, acc + 1);
}

fn main() -> i64 {
    print(count(1000000 as i64, 0 as i64));
    return 0 as i64;
}
